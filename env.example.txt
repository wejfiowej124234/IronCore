# ============================================
# Blockchain Wallet - Environment Variables
# Updated: 2025-11-07
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env file to version control!
# Last updated: November 2025

# ============================================
# REQUIRED - Application will not start without these
# ============================================

# Encryption key for wallet storage (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
WALLET_ENC_KEY=RDU3il552PpK2r0VsYY4UuLClwVDlL2XQHSPFRdKrjA=

# API authentication key (change in production!)
# Generate with: openssl rand -hex 32
API_KEY=dev_api_key_change_in_production

# JWT secret for token signing (minimum 256 bits)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_at_least_32_characters_long_change_this

# ============================================
# DATABASE CONFIGURATION
# ============================================

# SQLite (Development & Small deployments)
DATABASE_URL=sqlite://./data/wallet.db

# PostgreSQL (Production recommended)
# DATABASE_URL=postgresql://username:password@localhost/wallet_db

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server host (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
WALLET_HOST=127.0.0.1

# Server port (8888 for development, 8080 for production/Docker)
WALLET_PORT=8888

# ============================================
# CORS CONFIGURATION
# ============================================

# Frontend URL (for CORS)
# Development: http://localhost:3000
# Production: https://app.yourdomain.com
CORS_ALLOW_ORIGIN=http://localhost:3000

# ============================================
# SECURITY CONFIGURATION
# ============================================

# bcrypt password hashing cost (10-14 recommended)
BCRYPT_COST=12

# PBKDF2 key derivation iterations (100000+ recommended)
PBKDF2_ITERATIONS=100000

# Session timeout (in seconds, default: 900 = 15 minutes)
SESSION_TIMEOUT=900

# Account lockout threshold (failed login attempts)
LOCKOUT_THRESHOLD=5

# Account lockout duration (in seconds, default: 900 = 15 minutes)
LOCKOUT_DURATION=900

# ============================================
# BLOCKCHAIN RPC ENDPOINTS
# ============================================

# Ethereum Mainnet
ETHEREUM_RPC_URL=https://eth.llamarpc.com

# Polygon Mainnet
POLYGON_RPC_URL=https://polygon-rpc.com

# Binance Smart Chain
BSC_RPC_URL=https://bsc-dataseed.binance.org

# Sepolia Testnet (for testing)
SEPOLIA_RPC_URL=https://rpc.sepolia.org

# BSC Testnet (for testing)
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# ============================================
# OPTIONAL - LOGGING
# ============================================

# Rust log level (trace, debug, info, warn, error)
RUST_LOG=info

# ============================================
# OPTIONAL - MONITORING
# ============================================

# Prometheus metrics
METRICS_ENABLED=true
METRICS_PORT=9090

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================

# 1. Generate strong random keys (NEVER use these examples in production!)
# 2. Use HTTPS in production
# 3. Use PostgreSQL for production
# 4. Set RUST_LOG=warn in production
# 5. Use secrets management service (AWS Secrets Manager, Vault, etc.)
